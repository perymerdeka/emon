# Environment variables for the Personal Finance API

# --- Database Configuration ---
# Choose ONE of the following DATABASE_URL examples or provide your own.
# Ensure the corresponding driver is installed (see requirements.txt).

# SQLite (Async - Default if nothing set)
# DATABASE_URL=sqlite+aiosqlite:///./database.db

# SQLite (Sync)
# DATABASE_URL=sqlite:///./database.db

# PostgreSQL (Async) - Replace user, password, host, dbname
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/finance_db

# PostgreSQL (Sync) - Replace user, password, host, dbname
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/finance_db

# MySQL (Async) - Replace user, password, host, dbname
# DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/finance_db

# MySQL (Sync) - Replace user, password, host, dbname
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/finance_db


# --- Async DB Setting (Optional) ---
# Explicitly set to True or False to override inference from DATABASE_URL prefix.
# USE_ASYNC_DB=True


# --- JWT Configuration ---
# IMPORTANT: Replace with a strong, randomly generated secret key!
# Use `openssl rand -hex 32` to generate one. Keep this secret!
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Application Settings ---
# DEBUG=True # Set to False in production
